#!/bin/bash
# armada-node-report — report node status to Armada
# Usage: armada-node-report active "fixing bug"
#        armada-node-report idle
#        armada-node-report result "42"

STATUS="${1:-idle}"
MESSAGE="${2:-${STATUS}}"

armada report "$STATUS" "$MESSAGE" 2>/dev/null || true
